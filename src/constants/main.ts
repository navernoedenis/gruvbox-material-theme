import { type ThemeOptions } from '../types';

export const extensionName = 'gruvbox-material-theme';

export const defaultThemeOptions: ThemeOptions = {
  contrast: 'medium',
  cursor: 'white',
  enableSparators: false,
  palette: 'material',
  selection: 'grey',
};
